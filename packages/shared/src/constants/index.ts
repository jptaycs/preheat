/** Minutes per preheat slot (default, overridable via admin settings) */
export const DEFAULT_SLOT_DURATION_MINUTES = 30

/** Minutes before departure that confirmation window opens */
export const CONFIRMATION_WINDOW_MINUTES = 35

/** Minutes before departure that unconfirmed slots are auto-canceled */
export const AUTO_CANCEL_THRESHOLD_MINUTES = 5

/** Default target preheat temperature in °C */
export const DEFAULT_TARGET_TEMP_CELSIUS = 5

/** Max queue length per day (default) */
export const DEFAULT_MAX_QUEUE_LENGTH = 20
